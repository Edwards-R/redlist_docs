# Python Projects Readme
Dependencies are managed by `pdm`

Run `load_data.py` to load data from Filemaker exports into the sqlite database
Run `write_outputs.py` to write the sqlite database to the markdown and Excel files