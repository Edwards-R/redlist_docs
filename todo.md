# to do

Section on what data will be used for an analysis using iRecord data and why.

- how many records will be used?
- Check impact per taxon after doing first Red List pass

## redlist docs

Put executive summary at start of document (separate)

Comparison to iRecord data

- number of records per slice
- tetrad count
- mcp
- buffer union

raw values & percentages

## The Great Resort
This is a mess and I can't keep track of anything.

- Move anything not being used to a `depr` folder
- Sort the python out so that it belongs in this project
- Sort the *other* python code out so that it belongs in this project
- Sort the `make` processes out so that they automate as much of the pipeline as possible
- Put all docs that need to be 'public' into `docs`
- Anything left over, check if it needs to exist